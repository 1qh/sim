{
  "name": "backend",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "codegen": "bunx convex codegen --typecheck disable",
    "test": "bun test"
  },
  "dependencies": {
    "@auth/core": "latest",
    "@convex-dev/auth": "latest",
    "convex": "latest"
  },
  "devDependencies": {
    "convex-test": "latest"
  }
}
