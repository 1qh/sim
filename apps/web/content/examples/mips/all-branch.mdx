---
title: "all-branch"
slug: all-branch
kind: mips
tags: [mips, example]
---

# all-branch

MIPS program — all branch.

```asm-mips
addi $t0, $zero, 2
beq $t0, $zero, a
bne $t0, $zero, b
a:
j c
b:
addi $t0, $t0, -1
beq $t0, $zero, c
j b
c:
```
