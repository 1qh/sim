---
title: "string-length"
slug: string-length
kind: mips
tags: [mips, example]
---

# string-length

MIPS program — string length.

```asm-mips
add $t1, $zero, $zero
sl:
lw $t2, 0($t0)
beq $t2, $zero, sd
addi $t1, $t1, 1
addi $t0, $t0, 4
j sl
sd:
```
