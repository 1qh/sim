---
title: "factorial"
slug: factorial
kind: mips
tags: [mips, example]
---

# factorial

MIPS program — factorial.

```asm-mips
addi $t0, $zero, 5
addi $t1, $zero, 1
f:
beq $t0, $zero, end
add $t1, $t1, $t1
addi $t0, $t0, -1
j f
end:
```
