---
title: "array-sum"
slug: array-sum
kind: mips
tags: [mips, example]
---

# array-sum

MIPS program — array sum.

```asm-mips
addi $t2, $zero, 4
add $t1, $zero, $zero
sum:
lw $t3, 0($t0)
add $t1, $t1, $t3
addi $t0, $t0, 4
addi $t2, $t2, -1
bne $t2, $zero, sum
```
