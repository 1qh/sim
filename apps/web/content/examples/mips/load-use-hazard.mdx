---
title: "load-use-hazard"
slug: load-use-hazard
kind: mips
tags: [mips, example]
---

# load-use-hazard

MIPS example program: load-use-hazard.

```asm-mips
addi $t0, $zero, 1
add $t1, $t0, $t0
```
